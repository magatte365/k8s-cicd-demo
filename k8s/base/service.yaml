apiVersion: v1
kind: Service
metadata:
  name: k8s-cicd-demo
  labels:
    app: k8s-cicd-demo
spec:
  type: NodePort
  selector:
    app: k8s-cicd-demo
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
