apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

bases:
  - ../../base

namePrefix: dev-

replicas:
  - name: taskmanager-frontend
    count: 1

commonLabels:
  environment: dev
