apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

bases:
  - ../../base

namePrefix: dev-

replicas:
  - name: taskmanager-backend
    count: 1

commonLabels:
  environment: dev

patchesStrategicMerge:
  - patch-mongodb-uri.yaml
