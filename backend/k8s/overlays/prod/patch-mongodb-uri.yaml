apiVersion: apps/v1
kind: Deployment
metadata:
  name: taskmanager-backend
spec:
  template:
    spec:
      containers:
      - name: backend
        env:
        - name: MONGODB_URI
          value: "mongodb://prod-mongodb-service:27017/taskmanager"
