apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: backend
    component: taskmanager
spec:
  type: ClusterIP
  selector:
    app: backend
    component: taskmanager
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
